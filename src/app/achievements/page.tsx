"use client";

import { useEffect, useMemo, useState } from "react";
import { achievementDefinitions } from "@/lib/achievementEngine";
import { readDemoProfile } from "@/lib/demoMode";

type Filter = "all" | "unlocked" | "in-progress" | "locked";

export default function AchievementsPage() {
  const [filter, setFilter] = useState<Filter>("all");
  const [demoKeys, setDemoKeys] = useState<string[]>([]);
  const [demoActive, setDemoActive] = useState(false);
  useEffect(() => { const frame = window.requestAnimationFrame(() => { const demo = readDemoProfile(); setDemoKeys(demo?.achievementKeys ?? []); setDemoActive(Boolean(demo?.enabled)); }); return () => window.cancelAnimationFrame(frame); }, []);
  const summary = useMemo(() => ({ unlocked: achievementDefinitions.filter((achievement) => demoKeys.includes(achievement.key)).length, hidden: achievementDefinitions.filter((achievement) => achievement.hidden && !demoKeys.includes(achievement.key)).length, categories: new Set(achievementDefinitions.map((achievement) => achievement.category)).size }), [demoKeys]);
  const cards = useMemo(() => achievementDefinitions.filter((achievement) => {
    const unlocked = demoKeys.includes(achievement.key);
    const status: Filter = unlocked ? "unlocked" : achievement.hidden ? "locked" : "in-progress";
    return filter === "all" || status === filter;
  }), [demoKeys, filter]);

  return <main className="mx-auto max-w-6xl px-4 py-10 sm:px-6 lg:px-8"><p className="text-sm font-semibold uppercase text-emerald-700">Achievements</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Build learning momentum</h1><p className="mt-3 max-w-3xl text-slate-600">Achievements mark meaningful learning actions—not official SAT scores. They stay local in the fictional demo until cloud achievement tracking is configured.</p>{demoActive && <p className="mt-4 w-fit rounded-full bg-indigo-50 px-3 py-1 text-sm font-bold text-indigo-800">Fictional demo profile · Demo preview</p>}<section className="mt-7 grid gap-3 sm:grid-cols-3" aria-label="Achievement summary"><Summary label="Unlocked" value={`${summary.unlocked}/${achievementDefinitions.length}`} detail={demoActive ? "shown in demo data" : "local tracking begins in demo mode"} /><Summary label="Learning areas" value={String(summary.categories)} detail="practice, improvement, visual reasoning, and more" /><Summary label="Hidden goals" value={String(summary.hidden)} detail="revealed when their learning action is complete" /></section><div className="mt-7 flex flex-wrap gap-2" aria-label="Achievement filter">{(["all", "unlocked", "in-progress", "locked"] as Filter[]).map((option) => <button key={option} type="button" aria-pressed={filter === option} onClick={() => setFilter(option)} className={`min-h-11 rounded-md px-3 py-2 text-sm font-semibold ${filter === option ? "bg-emerald-600 text-white" : "border border-slate-300 text-slate-700 hover:bg-slate-50"}`}>{option.replace("-", " ")}</button>)}</div><section className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{cards.map((achievement) => { const unlocked = demoKeys.includes(achievement.key); const status = unlocked ? "Unlocked" : achievement.hidden ? "Hidden" : demoActive ? "Demo preview" : "In progress"; return <article key={achievement.key} className={`rounded-xl border p-5 shadow-sm ${unlocked ? "border-emerald-200 bg-emerald-50" : "border-slate-200 bg-white"}`}><div className="flex items-start justify-between gap-3"><div><p className="text-2xl" aria-hidden>🏅</p><p className="mt-3 text-xs font-semibold uppercase tracking-wide text-emerald-700">{achievement.category}</p></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold text-slate-700">{status}</span></div><h2 className="mt-3 font-bold text-slate-950">{achievement.hidden && !unlocked ? "Hidden achievement" : achievement.name}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{achievement.hidden && !unlocked ? "Keep practicing to reveal this achievement." : achievement.description}</p><div className="mt-4 flex items-center justify-between rounded-lg bg-slate-50 px-3 py-2 text-sm"><span className="font-semibold text-slate-700">{achievement.xpReward} XP</span><span className="text-slate-600">Goal: {achievement.target}</span></div>{unlocked ? <p className="mt-3 text-sm font-semibold text-emerald-800">Unlocked in fictional demo data</p> : <p className="mt-3 text-sm text-slate-600">{demoActive ? "Preview the learning goal in the fictional demo." : "Progress will appear here when achievement tracking is configured."}</p>}</article>; })}</section>{cards.length === 0 && <p className="mt-8 rounded-lg border border-slate-200 bg-white p-5 text-slate-600">No achievements match this filter. Choose All to view the full catalog.</p>}</main>;
}

function Summary({ label, value, detail }: { label: string; value: string; detail: string }) { return <article className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs font-semibold uppercase text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold text-slate-950">{value}</p><p className="mt-1 text-sm text-slate-600">{detail}</p></article>; }
