"use client";
import { achievementDefinitions } from "@/lib/achievementEngine";
export default function AchievementsPage() { return <main className="mx-auto max-w-6xl px-4 py-10"><p className="text-sm font-semibold uppercase text-emerald-700">Achievements</p><h1 className="mt-2 text-3xl font-bold">Build learning momentum</h1><p className="mt-3 text-slate-600">Achievements are earned from verified sessions after cloud setup. Hidden badges remain a mystery until unlocked.</p><section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{achievementDefinitions.map((achievement) => <article key={achievement.key} className="rounded-xl border border-slate-200 bg-white p-5"><p className="text-2xl" aria-hidden>🏅</p><h2 className="mt-3 font-bold">{achievement.hidden ? "Hidden achievement" : achievement.name}</h2><p className="mt-2 text-sm text-slate-600">{achievement.hidden ? "Keep learning to reveal this badge." : achievement.description}</p><p className="mt-3 text-xs font-semibold uppercase text-emerald-700">{achievement.xpReward} XP · {achievement.category}</p></article>)}</section></main>; }
