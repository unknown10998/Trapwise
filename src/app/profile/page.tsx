"use client";
import { ProtectedPage } from "@/components/ProtectedPage";
import { useAuth } from "@/components/AuthProvider";
export default function ProfilePage() { const { user } = useAuth(); return <ProtectedPage><main className="mx-auto max-w-5xl px-4 py-10"><p className="text-sm font-semibold uppercase text-emerald-700">Your profile</p><h1 className="mt-2 text-3xl font-bold">{String(user?.user_metadata.display_name ?? "Trapwise learner")}</h1><p className="mt-3 text-slate-600">Your private profile will show cloud-backed level, XP, streaks, skills, achievements, and visual-reasoning progress after your Supabase database is configured.</p><section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Card label="Level" value="1"/><Card label="XP" value="0"/><Card label="Current streak" value="0 days"/><Card label="Achievements" value="0 unlocked"/></section></main></ProtectedPage>; }
function Card({ label, value }: { label: string; value: string }) { return <article className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-xs font-semibold uppercase text-slate-500">{label}</p><p className="mt-2 text-xl font-bold">{value}</p></article>; }
