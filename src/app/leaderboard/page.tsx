"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { useAuth } from "@/components/AuthProvider";
import { readDemoProfile } from "@/lib/demoMode";

const rows = [{ rank: 1, name: "Jordan S.", level: 5, score: "920 XP", streak: 8 }, { rank: 2, name: "Mina R.", level: 4, score: "810 XP", streak: 6 }, { rank: 3, name: "Theo K.", level: 4, score: "760 XP", streak: 4 }, { rank: 4, name: "Avery Park (Demo)", level: 3, score: "640 XP", streak: 5 }];
const medal = ["🥇", "🥈", "🥉"];

export default function LeaderboardPage() {
  const { configured, user } = useAuth();
  const [demoActive, setDemoActive] = useState(false);
  useEffect(() => { const frame = window.requestAnimationFrame(() => setDemoActive(Boolean(readDemoProfile()?.enabled))); return () => window.cancelAnimationFrame(frame); }, []);
  return <main className="mx-auto max-w-5xl px-4 py-10 sm:px-6 lg:px-8"><div className="flex flex-wrap items-center gap-3"><p className="text-sm font-semibold uppercase text-emerald-700">Leaderboard</p><span className="rounded-full bg-indigo-700 px-3 py-1 text-xs font-bold tracking-wide text-white">DEMO DATA</span></div><h1 className="mt-2 text-3xl font-bold text-slate-950">Celebrate consistent learning</h1><p className="mt-3 max-w-2xl text-slate-600">This board uses fictional demonstration profiles. It never shows emails, internal IDs, private Mistake Twin details, or account information.</p><section className="mt-7 rounded-xl border border-indigo-200 bg-indigo-50 p-4 text-sm text-indigo-950"><strong>Weekly XP · fictional demo data.</strong> Future opt-in boards can recognize improvement, streaks, and Trap Forge—not only mastery.</section><section className="mt-7 grid gap-4 sm:grid-cols-3" aria-label="Top three demo learners">{rows.slice(0, 3).map((row, index) => <article key={row.rank} className={`rounded-xl border p-5 text-center shadow-sm ${index === 0 ? "border-amber-300 bg-amber-50" : "border-slate-200 bg-white"}`}><p className="text-3xl" aria-hidden>{medal[index]}</p><p className={`mt-3 text-xs font-bold uppercase ${index === 0 ? "text-amber-950" : "text-slate-500"}`}>Rank {row.rank}</p><h2 className="mt-1 font-bold text-slate-950">{row.name}</h2><p className="mt-3 text-2xl font-bold text-emerald-800">{row.score}</p><p className={`mt-1 text-sm ${index === 0 ? "text-amber-950" : "text-slate-600"}`}>Level {row.level} · {row.streak}-day streak</p></article>)}</section><section className="mt-6 rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="font-bold text-slate-950">All demo learners</h2><p className="mt-1 text-sm text-slate-600">A compact card layout keeps ranks readable on smaller screens.</p></div><span className="text-sm font-semibold text-slate-600">{rows.length} fictional profiles</span></div><ol className="mt-5 grid gap-3">{rows.map((row) => <li key={row.rank} className={`flex flex-col gap-3 rounded-lg border p-4 sm:flex-row sm:items-center sm:justify-between ${demoActive && row.rank === 4 ? "border-indigo-400 bg-indigo-50 ring-2 ring-indigo-200" : "border-slate-200 bg-slate-50"}`} aria-label={`Rank ${row.rank}: ${row.name}, level ${row.level}, ${row.score}, ${row.streak}-day streak`}><div className="flex items-center gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-white text-sm font-bold text-slate-950 shadow-sm">#{row.rank}</span><div><p className="font-semibold text-slate-950">{row.name}{demoActive && row.rank === 4 ? <span className="ml-2 text-sm text-indigo-800">Your demo profile</span> : null}</p><p className="mt-1 text-sm text-slate-600">Level {row.level} · {row.streak}-day streak</p></div></div><strong className="text-lg text-emerald-800">{row.score}</strong></li>)}</ol></section>{!configured && <p className="mt-5 text-sm text-slate-600">This is a safe guest demo. Cloud rankings remain opt-in and are not required to test Trapwise.</p>}{user && <Link href="/settings" className="mt-4 inline-flex font-semibold text-emerald-700 hover:underline">Open privacy settings</Link>}</main>;
}
