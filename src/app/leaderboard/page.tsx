"use client";
import Link from "next/link";
import { useAuth } from "@/components/AuthProvider";
export default function LeaderboardPage() { const { configured, user } = useAuth(); const rows = [["1", "Jordan S.", "920 XP"], ["2", "Mina R.", "810 XP"], ["3", "Theo K.", "760 XP"], ["4", "Avery Park (Demo)", "640 XP"]]; return <main className="mx-auto max-w-5xl px-4 py-10"><p className="text-sm font-semibold uppercase text-emerald-700">Leaderboard</p><h1 className="mt-2 text-3xl font-bold">Celebrate consistent learning</h1><p className="mt-3 text-slate-600">This demo board shows fictional display names and verified-style XP only—never emails or Mistake Twin details.</p><section className="mt-8 rounded-xl border bg-white p-5"><h2 className="font-bold">Weekly XP · Fictional demo data</h2><ol className="mt-4 grid gap-2">{rows.map(([rank, name, score]) => <li key={rank} className="flex justify-between rounded-md bg-slate-50 p-3"><span>#{rank} · {name}</span><strong>{score}</strong></li>)}</ol></section>{!configured && <p className="mt-5 text-sm text-slate-600">Connect Supabase to replace this fictional demo board with opt-in, privacy-safe cloud rankings.</p>}{user && <Link href="/settings" className="mt-4 inline-flex font-semibold text-emerald-700">Open privacy settings</Link>}</main>; }
